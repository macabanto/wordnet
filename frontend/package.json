{
  "name": "@wordnet/frontend",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist -b gh-pages -r https://github.com/macabanto/wordnet.git --dotfiles"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^7.1.0"
  },
  "dependencies": {
    "three": "^0.179.1"
  }
}